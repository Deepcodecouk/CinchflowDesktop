# Experiments
Choose one incomplete task from the list below and implement it. Make sure to follow clean coding practices, ensure that SOLID is adhered to and use components in separate files where it makes sense to do so. Abstract common components that may be used across different features into components/ui as needed, extract components specific to the feature into functional folders (e.g. cashflow components into components/cashflow).

Make sure to use good component decomposition as much as possible.

Do not create event handlers inline with markup, use named functions.

When task is complete, but do not commit or push.

After each task is complete, it will be evaluated (by the user) to determine if is should be discarded or refined, committed and pushed.

# Experiments - cashflow showing multiple accounts.
[] We currently allow the user to switch between accounts to view the cashflow of. I'd like the user to be able to turn on/off visibility of accounts to show just the ones they are interested in budgetting against. Make the account selection drop down a multi select and bring in the cashflow view for each account selected. Remember which accounts they have selected using local storage. Each account should show in a separate cashflow table with a header and some vertical spacing and the "toggle all" option which expands/collapses all category headers should be moved to the account header row.




